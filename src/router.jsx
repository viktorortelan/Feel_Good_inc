import { BrowserRouter, Routes, Route } from "react-router-dom";

export default function Site() {
    return(
        <BrowserRouter>
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  );
}